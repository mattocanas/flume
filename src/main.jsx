import React from "react";
import ReactDOM from "react-dom/client";
import FlowCytometryApp from "./flow_combined.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FlowCytometryApp />
  </React.StrictMode>
);
